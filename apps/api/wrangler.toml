name = "kano-proxy"
main = "src/index.ts"
compatibility_date = "2025-02-24"
compatibility_flags = ["nodejs_compat"]

[[d1_databases]]
binding = "DB"
database_name = "kano-proxy"
database_id = "local-kano-proxy-placeholder"
migrations_dir = "migrations"

[[kv_namespaces]]
binding = "BENCH"
id = "local-kano-proxy-bench-placeholder"

[[kv_namespaces]]
binding = "CACHE"
id = "local-kano-proxy-cache-placeholder"

[vars]
# Local: land on Vite admin UI after Google OAuth.
# Production: set APP_URL / GOOGLE_REDIRECT_URI to https://<your-domain> (wrangler secret/vars or Dashboard).
APP_URL = "http://127.0.0.1:5173"
GOOGLE_REDIRECT_URI = "http://127.0.0.1:8787/api/auth/callback"
# Codex public client only allows this registered redirect (lincy). Manual paste completes the flow.
CODEX_REDIRECT_URI = "http://localhost:1455/auth/callback"

# Secrets via .dev.vars / wrangler secret:
# GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, SESSION_SECRET, TOKEN_ENCRYPTION_KEY
