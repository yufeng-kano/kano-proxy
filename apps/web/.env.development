# Session cookie is set on the API host after Google OAuth — use the same origin for API calls.
VITE_API_ORIGIN=http://127.0.0.1:8787

# Contact address shown in the public footer. Set per deploy.
VITE_CONTACT_EMAIL=yuufeng.kano@gmail.com
