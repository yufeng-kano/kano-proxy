# Crawling is allowed everywhere. Admin routes are kept out of indexes with
# X-Robots-Tag: noindex (see _headers), not with Disallow: a disallowed URL can
# still be indexed from links, a noindex one cannot. No Sitemap line: the
# directive needs an absolute URL and this file carries no hostname; submit
# /docs/sitemap.xml in Search Console instead. See docs/docs-site.md.
User-agent: *
Allow: /
