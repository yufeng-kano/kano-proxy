# Admin routes are kept out of indexes with X-Robots-Tag: noindex (see
# _headers), not with Disallow: a disallowed URL can still be indexed from
# links, a noindex one cannot. The Worker-routed API prefixes below never reach
# Pages, so _headers cannot cover them; they carry no content, and Disallow is
# the right tool for an API surface. No Sitemap line: the directive needs an
# absolute URL and this file carries no hostname; submit /docs/sitemap.xml in
# Search Console instead. See docs/docs-site.md.
User-agent: *
Disallow: /openai/
Disallow: /anthropic/
Disallow: /g/
Disallow: /api/
Disallow: /agent/
Allow: /
